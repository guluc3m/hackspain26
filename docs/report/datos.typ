// Generado por albertitos.report_data — NO editar a mano.
// Cada métrica es (valor, etiqueta) con etiqueta 'medido' o 'sin datos'.
#let facturasDecididas = ("0", "medido")
#let conteoResultados = (
  "PAGAR": ("0", "medido"),
  "NO_PAGAR": ("0", "medido"),
  "ESCALAR": ("0", "medido"),
)
#let paginasExtraccion = ("0", "medido")
#let latenciaMedia = ("sin datos medidos todavía", "sin datos")
#let costeAcumulado = ("sin datos medidos todavía", "sin datos")
#let versionReglas = ("sin datos medidos todavía", "sin datos")
#let facturasPorSegundo = ("sin datos medidos todavía", "sin datos")
#let costePorFactura = ("sin datos medidos todavía", "sin datos")
#let extractores = (:)
#let reintentos = ("0", "medido")
#let omisiones = ("0", "medido")
#let erroresProveedor = ("0", "medido")
