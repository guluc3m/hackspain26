# slides

## Compilar

```sh
cd ..
typst compile --font-path slides/fonts slides/slides.typ --root=.
```

Requiere [Typst](https://typst.app) ≥ 0.13. Las fuentes (Bungee, DM Sans) están
incluidas en `fonts/`.

